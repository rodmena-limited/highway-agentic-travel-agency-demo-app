# Highway Agentic Bot Demo Configuration
# This app uses ONLY public APIs - no engine internals access

# Highway API endpoint
HIGHWAY_API_URL=https://highway.rodmena.app/api/v1

# Demo tenant API key
# Generate via: hwe apikey create --tenant demo --permissions ALL --expiry 6m
HIGHWAY_API_KEY=hw_k1_your_api_key_here

# RAG Knowledge Base storage file ID (upload travel docs first)
RAG_STORAGE_ID=your-storage-file-uuid-here

# Flask configuration
FLASK_APP=app.py
FLASK_DEBUG=1
FLASK_PORT=5001

# Webhook URL for workflow callbacks (must be accessible from Docker containers)
WEBHOOK_BASE_URL=https://your-domain.example.com
